import React from "react";
import ReactDOM from "react-dom";
import FrontendInfo from "./FrontendInfo"; // Assuming FrontendInfo is in the same directory

ReactDOM.render(
  <React.StrictMode>
    <FrontendInfo />
  </React.StrictMode>,
  document.getElementById("root")
);